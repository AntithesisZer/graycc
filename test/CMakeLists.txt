add_test(
    NAME test1
    COMMAND ${CMAKE_CURRENT_SOURCE_DIR}/test1.sh
)
set_tests_properties(test1
    PROPERTIES
        WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
)

file(RELATIVE_PATH REL_TMP  "${CMAKE_CURRENT_BINARY_DIR}" "${CMAKE_CURRENT_SOURCE_DIR}/tmp")
file(RELATIVE_PATH REL_TMPS "${CMAKE_CURRENT_BINARY_DIR}" "${CMAKE_CURRENT_SOURCE_DIR}/tmp.s")
set_directory_properties(PROPERTIES
    ADDITIONAL_CLEAN_FILES "${REL_TMP};${REL_TMPS}"
)